mutation CreateTreatment(
  $frequency: Int!
  $phase: Int!
  $duration: Int!
  $priceRangeName: String!
) {
  createTreatment(
    input: {
      frequency: $frequency
      phase: $phase
      duration: $duration
      priceRangeName: $priceRangeName
    }
  )
}
