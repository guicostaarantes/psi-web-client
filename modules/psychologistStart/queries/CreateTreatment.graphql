mutation CreateTreatment(
  $frequency: Int!
  $phase: Int!
  $duration: Int!
  $price: Int!
) {
  createTreatment(
    input: {
      frequency: $frequency
      phase: $phase
      duration: $duration
      price: $price
    }
  )
}
